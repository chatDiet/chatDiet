name: 'deploy'

on:
  push:
    branches: [testDevelop]

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@v3
      - name: Use Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '18.10.0'
      - name: Install Dependencies
        run: npm install
      - name: Npm Build
        run: unset CI && npm run build

  docker:
    name: Deploy Docker Image
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_ACCESS_KEY }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Build and push
        uses: docker/build-push-action@v3
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: byeongil/chatdiet:test1
# # 배포
#   deploy:
#     needs: build # build 후에 실행되도록 정의
#     name: Deploy
#     runs-on: [self-hosted, label-go] # AWS ./configure에서 사용할 label명
#     steps:
#       - name: Login to ghcr
#         uses: docker/login-action@v1
#         with:
#           registry: ghcr.io
#           username: ${{ github.actor }}
#           password: ${{ secrets.GHCR_TOKEN }}
#       - name: Docker run
#         run: |
#           docker stop my-web && docker rm my-web && docker rmi ${{ secrets.DOCKERHUB_USERNAME }}/chatdiet:test1

#           docker pull ${{ secrets.DOCKERHUB_USERNAME }}/chatdiet:test1

#           docker run -d -p 80:80 --name my-web ${{ secrets.DOCKERHUB_USERNAME }}/chatdiet:test1
